void main(){
  
/*Ternary Operator  
   Condition ? Answer is true: Answer is false
 */ 
    int age=18;
    
  String response=age>=18 ? "can vote" :"cannot vote";
  
  print(response);
 print(age>=18 ? "Can Vote" : "Cannot vote");


  int num1=35;
  int num2=30;
  String result=num1>=num2 ? "Smaller" : "larger";
  
  print(result);

  
}